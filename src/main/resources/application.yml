server:
    port: 8081
spring:
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: ${ENTRA_ISSUER_URI}
                    audiences:
                        - ${ENTRA_API_CLIENT_ID}

microservices:
    clientes:
        base-url: http://localhost:8083